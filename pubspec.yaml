name: evoting
description: E voting using blockchain

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  google_fonts: ^2.3.1

  dio: ^4.0.4

  bloc: ^8.0.3
  flutter_bloc: ^8.0.1


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.4

flutter:

  uses-material-design: true

  assets:
    - assets/
